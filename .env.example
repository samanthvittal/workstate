# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings
# PostgreSQL (default for both development and production)
DB_NAME=workstate
DB_USER=workstateadmin
DB_PASSWORD=admin123
DB_HOST=localhost
DB_PORT=5432

# Redis Settings
# Used for caching and Celery message broker
REDIS_URL=redis://127.0.0.1:6379/1
CELERY_BROKER_URL=redis://127.0.0.1:6379/0
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/0

# Email Settings
# Console backend for development (prints emails to console)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# SMTP backend for production
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-app-password
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL=noreply@workstate.com

# Media Files
# Path for user-uploaded files (avatars, etc.)
# Default: media/ directory in project root
# MEDIA_ROOT=/path/to/media
# MEDIA_URL=/media/

# Session Settings
# Session cookie age in seconds (default: 2 weeks)
# SESSION_COOKIE_AGE=1209600

# Security Settings (for production)
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_BROWSER_XSS_FILTER=True
# SECURE_CONTENT_TYPE_NOSNIFF=True
